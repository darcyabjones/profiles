alias la='ls -la'
alias ll='ls -l'

if [ -e /usr/bin/nvim ]; then
    alias vim='nvim'
elif [ -e /usr/bin/gvim ]; then
    alias vim='gvim -v'
fi

start_conda () {
    source /home/darcyabjones/miniconda3/etc/profile.d/conda.sh
}
