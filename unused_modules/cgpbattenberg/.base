#%Module1.0#####################################################################
##
## $name modulefile
##

set ver [lrange [split [ module-info name ] / ] 1 1 ]
set name [lrange [split [ module-info name ] / ] 0 0 ]

module-whatis   "Set environment variables to use $name version $ver"

if { ![ is-loaded perl ] } {
        module load perl
}

prepend-path PATH /usr/local/$name/$ver/bin

prepend-path PERL5LIB /usr/local/$name/$ver/lib/perl5
prepend-path PERL5LIB /usr/local/$name/$ver/lib/perl5/x86_64-linux-thread-multi-ld
